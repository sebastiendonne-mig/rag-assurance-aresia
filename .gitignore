# Secrets — ne jamais committer
.env*
!.env.example
.streamlit/secrets.toml

# Index Chroma — reconstruit au build Docker depuis data/chunks/chunks.json
# (voir Dockerfile), plus une copie locale de dev ne doit partir en prod
chroma_db/

# Environnement Python local
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Logs de debug Streamlit
data/*.log

# Résultats des évaluations Claude/Mistral sur le chemin upload (sous-lot 3.2).
# Ligne ciblée : les résultats du golden set de juillet
# (tests/results/golden_set_results_*.json) restent visibles pour git.
tests/results/upload_eval_*

# Artefacts macOS
.DS_Store

# Cache outils
.claude/
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
