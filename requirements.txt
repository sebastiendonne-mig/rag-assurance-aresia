# RAG agentique assurance — AssurConseil 365
#
# Déploiement cible : Google Cloud Run (build via Dockerfile, cf. `gcloud run
# deploy --source .`). Le Dockerfile installe ce fichier via
# `pip install -r requirements.txt` — pas de lecture de uv.lock. La ligne
# --extra-index-url ci-dessous force pip à résoudre torch depuis l'index
# CPU-only (comme le fait uv via [tool.uv.sources] dans pyproject.toml), pour
# éviter de télécharger les wheels CUDA (plusieurs Go, inutiles sur Cloud Run CPU).
#
# Installation locale équivalente : uv sync (respecte pyproject.toml + uv.lock)

--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.1
anthropic==0.111.0
langgraph==1.2.6
langchain-core>=1.4.0
chromadb==1.5.9
sentence-transformers==5.6.0
transformers==5.12.1
huggingface-hub==1.20.1
pypdf>=6.0.0
pdfplumber>=0.11.0
streamlit==1.58.0
python-dotenv>=1.2.0
psutil>=6.0.0
