# RAG agentique assurance — AssurConseil 365
#
# Sur Streamlit Community Cloud, ce fichier est IGNORÉ au profit de uv.lock
# (priorité 1 dans l'ordre de résolution Streamlit Cloud). La source d'index
# CPU-only pour torch est déclarée dans pyproject.toml via [tool.uv.sources].
#
# Ce fichier sert uniquement de référence pour installation manuelle locale :
#   pip install -r requirements.txt  (installe torch CUDA/standard depuis PyPI)
#   uv sync                          (respecte pyproject.toml + uv.lock)

torch==2.12.1
anthropic>=0.111.0
langgraph>=1.2.0
langchain-core>=1.4.0
chromadb>=1.5.0
sentence-transformers>=5.0.0
pypdf>=6.0.0
pdfplumber>=0.11.0
streamlit>=1.58.0
python-dotenv>=1.2.0
psutil>=6.0.0
