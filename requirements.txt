# RAG agentique assurance — AssurConseil 365
#
# torch CPU-only : l'index PyTorch CPU est le primaire pour éviter d'installer
# les variantes CUDA (~2 Go de nvidia-* / cuda-* inutiles sur Streamlit Cloud).
# PyPI reste disponible en extra-index pour tous les autres packages.
--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple/

torch==2.12.1
anthropic>=0.111.0
langgraph>=1.2.0
langchain-core>=1.4.0
chromadb>=1.5.0
sentence-transformers>=5.0.0
pypdf>=6.0.0
pdfplumber>=0.11.0
streamlit>=1.58.0
python-dotenv>=1.2.0
psutil>=6.0.0
